return {
  "barrett-ruth/live-server.nvim",
  -- NOTE: Installing global nixpkg on NixOS and npm module through Mise config on MacOS and Debian
  -- build = "npm i -g live-server",
  cmd = { "LiveServerStart", "LiveServerStop" },
  opts = {}
}
