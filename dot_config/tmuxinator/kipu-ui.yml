name: kipu-ui
root: <%= ENV["PROJECTS_DIR"] %>/kipu-ui
windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
  - components:
      layout: <%= ENV["MUX_LAYOUT"] %>
      root: <%= ENV["PROJECTS_DIR"] %>/kipu-ui/components
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
  - composables:
      layout: <%= ENV["MUX_LAYOUT"] %>
      root: <%= ENV["PROJECTS_DIR"] %>/kipu-ui/composables
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
  - stories:
      layout: <%= ENV["MUX_LAYOUT"] %>
      root: <%= ENV["PROJECTS_DIR"] %>/kipu-ui/stories
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
  - build:
      layout: <%= ENV["MUX_LAYOUT"] %>
      root: <%= ENV["PROJECTS_DIR"] %>/kipu-ui/build
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
  - test:
      layout: <%= ENV["MUX_LAYOUT"] %>
      root: <%= ENV["PROJECTS_DIR"] %>/kipu-ui/test
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
