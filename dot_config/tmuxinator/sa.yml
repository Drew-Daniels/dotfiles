name: sa
root: <%= ENV["PROJECTS_DIR"] %>
windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - cmd:
            - fish
            - cls
            - python -m stay-awake
