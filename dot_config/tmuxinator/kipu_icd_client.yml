name: kipu_icd_client
root: <%= ENV["PROJECTS_DIR"] %>/kipu_icd_client
windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
