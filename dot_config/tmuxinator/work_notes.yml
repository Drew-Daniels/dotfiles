name: work_notes
root: <%= ENV["PROJECTS_DIR"] %>/work_notes
windows:
  - 1)work_notes:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - work_notes:
            - fish
            - cls
            - nvim -c "SU td"
# don't really use this all that much
# - 2)retro:
#     layout: <%= ENV["MUX_LAYOUT"] %>
#     panes:
#       - retro:
#         - fish
#         - cls
#         - nvim -c "Retro csprint"
