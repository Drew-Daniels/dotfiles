name: work_notes
root: <%= ENV["PROJECTS_DIR"] %>/work_notes
windows:
  - 1)work_notes:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - work_notes:
            - fish
            - cls
            - nvim -c "SU td"
  - 2)project:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - retro:
            - fish
            - cls
            - nvim
