name: kipu_messaging_center
root: <%= ENV["PROJECTS_DIR"] %>/kipu_messaging_center
windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
            - fish
            - cls
        - cmd:
            - fish
            - cls
