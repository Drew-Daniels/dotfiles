name: fhir-package-loader
root: <%= ENV["PROJECTS_DIR"] %>/fhir-package-loader
windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
            - fish
            - cls
            - nvim
        - cmd:
            - fish
            - cls
