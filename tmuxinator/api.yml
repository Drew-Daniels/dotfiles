name: api
root: <%= ENV["PROJECTS_DIR"] %>/keet-api

windows:
  - editor:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - fish
  - stack:
      layout: <%= ENV["MUX_LAYOUT"] %> 
      panes:
        - ahoy up
