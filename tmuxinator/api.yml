name: api
root: <%= ENV["PROJECTS_DIR"] %>

windows:
  - editor:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - fish
  - stack:
      root: <%= ENV["PROJECTS_DIR"] %>/keet-api
      layout: <%= ENV["MUX_LAYOUT"] %> 
      panes:
        - ahoy up
