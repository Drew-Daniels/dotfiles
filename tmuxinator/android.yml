name: android
root: <%= ENV["PROJECTS_DIR"] %>/keet-mobile

windows:
  - admin:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - yarn && yarn android
        - fish
