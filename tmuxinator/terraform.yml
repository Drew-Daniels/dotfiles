name: terraform
root: <%= ENV["PROJECTS_DIR"] %>/terraform

windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - fish
          - cls
        - cmd:
          - fish
          - cls
  - portal-dev:
      root: <%= ENV["PROJECTS_DIR"] %>/terraform/applications/portal/development
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - fish
          - cls
        - cmd:
          - fish
          - cls
  - portal-prd:
      root: <%= ENV["PROJECTS_DIR"] %>/terraform/applications/portal/production
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - fish
          - cls
        - cmd:
          - fish
          - cls
  - common:
      layout: <%= ENV["MUX_LAYOUT"] %>
      root: <%= ENV["PROJECTS_DIR"] %>/terraform/common
      panes:
        - editor:
          - fish
          - cls
        - cmd:
          - fish
          - cls
