name: embedded-local
root: <%= ENV["PROJECTS_DIR"] %>/keet-embedded

windows:
  - embedded:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - yarn env:local && yarn && yarn start
        - fish
