name: embedded-local
root: <%= ENV["PROJECTS_DIR"] %>

windows:
  - embedded:
      root: <%= ENV["PROJECTS_DIR"] %>/keet-embedded
      layout: main-vertical
      panes:
        - nvim
        - fish
        - yarn env:local && yarn start
