name: dotfiles
root: <%= ENV["PROJECTS_DIR"] %>/dotfiles
on_project_exit: clear

windows:
  - dotfiles:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - fish
          - cls
          - nvim
        - cmd:
          - fish
          - cls
