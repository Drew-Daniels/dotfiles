name: dotfiles
root: <%= ENV["PROJECTS_DIR"] %>/dotfiles

windows:
  - dotfiles:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - fish
          - cls
          - nvim
        - cmd:
          - fish
          - cls
