name: keetman
root: <%= ENV["PROJECTS_DIR"] %>/keetman

windows:
  - pt:
      layout: even-horizontal
      panes:
        - editor:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - fish
          - nvim
        - git:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - yarn
          - fish
        - cmd:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - fish
