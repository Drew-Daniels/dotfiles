name: keetman
root: <%= ENV["PROJECTS_DIR"] %>/keetman
on_project_exit: clear

windows:
  - pt:
      layout: even-horizontal
      panes:
        - editor:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - fish
          - nvim
        - cmd:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - fish
