name: ptnow-local
root: <%= ENV["PROJECTS_DIR"] %>/ptnow-patient

windows:
  - pt:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - yarn env:local && yarn start
        - fish
