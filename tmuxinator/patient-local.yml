name: patient-local
root: <%= ENV["PROJECTS_DIR"] %>/keet-patient

windows:
  - pt:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - yarn env:local && yarn && yarn start
        - fish
