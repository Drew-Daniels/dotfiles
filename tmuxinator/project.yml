name: <%= @settings["n"] %>
root: <%= ENV["PROJECTS_DIR"] %>/<%= @settings["d"] %>

windows:
  - root:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - fish
          - cls
          - nvim
        - dev:
          - fish
          - cls
        - cmd:
          - fish
          - cls
