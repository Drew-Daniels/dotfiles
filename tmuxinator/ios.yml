name: ios
root: <%= ENV["PROJECTS_DIR"] %>

windows:
  - admin:
      root: <%= ENV["PROJECTS_DIR"] %>/keet-mobile
      layout: main-vertical
      panes:
        - nvim
        - fish
        - yarn && yarn ios
