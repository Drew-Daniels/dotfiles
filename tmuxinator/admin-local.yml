name: admin-local
root: <%= ENV["PROJECTS_DIR"] %>/keet-admin

windows:
  - admin:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - yarn env:local && yarn start
        - fish
