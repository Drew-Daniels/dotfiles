name: admin-local
root: <%= ENV["PROJECTS_DIR"] %>

windows:
  - admin:
      root: <%= ENV["PROJECTS_DIR"] %>/keet-umi
      layout: main-vertical
      panes:
        - nvim
        - fish
        - yarn env:local && yarn start
