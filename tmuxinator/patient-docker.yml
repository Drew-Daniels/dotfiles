name: patient-docker

root: <%= ENV["PROJECTS_DIR"] %>/keet-patient

windows:
  - pt:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - nvim
        - fish
        - yarn env:docker && <%= ENV["EXPORT_E2E_CREDS_SCRIPT"] %> patient && yarn start
        - fish
