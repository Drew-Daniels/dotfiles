name: admin-docker
root: <%= ENV["PROJECTS_DIR"] %>/keet-admin

windows:
  - admin:
      layout: <%= ENV["MUX_LAYOUT"] %> 
      panes:
        - nvim
        - fish
        - yarn env:docker && yarn start
