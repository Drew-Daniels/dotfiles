name: admin-docker
root: <%= ENV["PROJECTS_DIR"] %>/keet-admin

windows:
  - admin:
      layout: <%= ENV["MUX_LAYOUT"] %> 
      panes:
        - nvim
        - fish
        - yarn env:docker && <%= ENV["EXPORT_E2E_CREDS_SCRIPT"] %> admin && yarn start
