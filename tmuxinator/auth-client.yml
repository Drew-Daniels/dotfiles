name: auth-client
root: <%= ENV["PROJECTS_DIR"] %>/keet-auth-client

windows:
  - pt:
      layout: <%= ENV["MUX_LAYOUT"] %>
      panes:
        - editor:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - fish
          - nvim
        - git:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - yarn
          - fish
        - cmd:
          - <%= ENV["MUXLSHELL_RUN_CMD"] %>
          - fish
